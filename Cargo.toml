[package]
name = "cache-models"
version = "0.1.0"
edition = "2024"
repository = "https://github.com/danielruss/cache-models"

[dependencies]
clap = { version = "4.6.7", features = ["derive"] }
soccer-cache = "0.1.0"

[profile.dist]
inherits = "release"
lto = "thin"
